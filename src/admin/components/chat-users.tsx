import { Avatar, AvatarFallback } from "@/components/ui/avatar";

export default function ChatsUsers() {
  return (
    <section>
      <Avatar>
        <AvatarFallback>A</AvatarFallback>
      </Avatar>
    </section>
  );
}
